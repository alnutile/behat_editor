<?php

/**
 * Some unit tests are in the tests folder of this module
 * The build process for composer-manager made this not feesable right now
 */

use Drupal\BehatEditor;

class SimpletestUnitTestBehatEditorCase extends DrupalUnitTestCase {

    public static function getInfo() {
      return array(
        'name' => 'BehatEditor unit tests',
        'description' => 'Test some file management features',
        'group' => 'BehatEditor',
      );
    }

    public function testFileCreation() {
        $results = new Drupal\BehatEditor\FileModel();
        $file = array();
        $this->assertTrue($result, $file);
    }
}